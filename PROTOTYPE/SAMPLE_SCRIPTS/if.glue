import /bin/ as b
import /usr/bin/ as ub
import /usr/local/bin/ as ulb

proc checkColnum a b:
	str c = ulb.retu < $a
	b.test "$c" = "$b"

proc main file num:
| checkColnum $file $num:
	b.echo "OK"
| othewise:
	b.false
