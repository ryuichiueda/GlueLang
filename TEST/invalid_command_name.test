#!/bin/bash 

tmp=/tmp/$$
scr=$(basename $0)

ERROR_CHECK(){
	[ "$(tr -d '0 ' <<< ${PIPESTATUS[@]} )" = "" ] && return
	rm -f $tmp-*
	echo "$scr" "$1"
	exit 1
}

../main ./invalid_command_name.glue > /dev/null
[ "$?" = 1 ]
ERROR_CHECK "TEST1 error"

rm -f $tmp-*
