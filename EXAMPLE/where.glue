import PATH

# "file a = ..." and "file b" = ... are evaluated before "diff a b"
diff local.a local.b
	where file a = seq 10
	      file b = seq 10

# "!> true" is required because this diff returns 1
file ans = diff local.c local.d !> true
	where file c = seq 8
	      file d = seq 10

cat ans
